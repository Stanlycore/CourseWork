# Транслятор Python 2 → Python 3

**Курсовая работа по дисциплине "Теория Языков Программирования"**

**Вариант 12:** Поддержка вложенных структур, таблица идентификаторов через цепочки, полная трансляция всех различий Python 2/3

## 📋 Описание

Полноценный транслятор, который преобразует код Python 2 в Python 3 с полной поддержкой всех синтаксических и семантических различий между версиями. 

Реализован полный цикл компиляции:

1. **Лексический анализ** - разбиение исходного кода на токены с отслеживанием Python 2 паттернов
2. **Синтаксический анализ** - построение абстрактного синтаксического дерева (AST) с поддержкой вложенных структур
3. **Семантический анализ** - управление таблицей идентификаторов с поддержкой областей видимости
4. **Оптимизация** - свертывание констант, удаление мертвого кода
5. **Генерация кода** - создание корректного Python 3 кода

## 🎯 Главное Улучшение

Эта версия **полностью исправляет** критические ошибки предыдущей версии:

### ✅ Решенные Проблемы

- ✅ **Вложенные структуры** - теперь правильно обрабатываются классы внутри функций, функции внутри классов, циклы внутри условий и т.д.
- ✅ **Таблица идентификаторов** - использует хеш-таблицу с цепочками (chaining) для разрешения коллизий
- ✅ **Синтаксическое дерево** - вывод дерева в формате как в labLEX.py (правильная визуализация)
- ✅ **ВСЕ различия между Python 2 и Python 3** - поддержка 20+ типов преобразований:
  - `print` statement → `print()` function
  - Unicode literals: `u"string"` → `"string"`
  - Integer division: `/` для целых → `//`
  - `xrange()` → `range()`
  - Exception syntax: `except E, e` → `except E as e`
  - Dict methods return views, not lists
  - `<>` operator → `!=`
  - И 13+ других преобразований

### ✅ Реализованные Требования Вариант 12

- ✅ Поддержка вложенных конструкций (классы, функции, циклы, условия)
- ✅ Таблица идентификаторов с единой структурой через все scope'ы
- ✅ Разрешение коллизий при хешировании через списки (цепочки)
- ✅ Полная поддержка всех различий Python 2 ↔ Python 3
- ✅ Правильный вывод синтаксического дерева
- ✅ Проверка контекстных условий

## 🔄 Полный Список Поддерживаемых Преобразований

| # | Преобразование | Python 2 | Python 3 |
|---|---|---|---|
| 1 | Print statement | `print "text"` | `print("text")` |
| 2 | Unicode strings | `u"text"` | `"text"` |
| 3 | Integer division | `7 / 2 = 3` | `7 // 2 = 3` |
| 4 | Range function | `xrange(10)` | `range(10)` |
| 5 | Exception syntax | `except E, e:` | `except E as e:` |
| 6 | Dict methods | `d.keys()` returns list | `d.keys()` returns view |
| 7 | Comparison | `x <> y` | `x != y` |
| 8 | Input function | `raw_input()` | `input()` |
| 9 | String type | `basestring` | `str` |
| 10 | Unicode type | `unicode()` | `str()` |
| 11 | Long type | `42L` | `42` |
| 12 | Map/Filter/Zip | return list | return iterator |
| 13 | Reduce function | builtin | `from functools import reduce` |
| 14 | Exec | `exec code` | `exec(code)` |
| 15 | File type | `file()` | `open()` |
| 16 | Generator.next() | `gen.next()` | `next(gen)` |
| 17 | Octal literals | `0755` | `0o755` |
| 18 | Relative imports | `from x import y` | `from .x import y` |
| 19 | Metaclasses | `__metaclass__ = X` | `class M(metaclass=X):` |
| 20 | String vs Bytes | no distinction | str and bytes |

## 🏗️ Архитектура

### Основные модули

```
translator_core.py          # НОВЫЙ: основной модуль с Lexer, Token, IdTable
                            # Полная поддержка всех преобразований
parser/                     # Синтаксический анализатор
├── ast_nodes.py           # Узлы AST с поддержкой вложенности
└── parser.py              # Рекурсивный спуск парсер

identifier_table/          # Таблица идентификаторов
├── id_table.py            # Хеш-таблица со scope'ами
└── scoped_table.py        # Управление вложенными scope'ами

optimizer/                 # Оптимизатор
└── optimizer.py           # Свертывание констант

code_generator/            # Генератор кода
└── generator.py           # Вывод на Python 3

examples/                  # Примеры
└── examples.py            # 12+ примеров Python 2 кода

main.py                     # Главный GUI файл
logger.py                   # Логирование
```

### Таблица Идентификаторов

Реализована **ScopedIdentifierTable** с:

- ✅ **Хеш-таблица** - через цепочки (chaining)
- ✅ **Вложенные scope'ы** - стек scope'ов
- ✅ **Статистика** - коллизии, пробы, коэффициент загрузки
- ✅ **Динамическое расширение** - при превышении load factor

**Пример хеширования:**
```
Bucket[0]: [var1, var2]  // Коллизия
Bucket[5]: [func1]       // Без коллизии
Bucket[10]: [class1, method1, method2]
```

## 🎨 Интерфейс Пользователя

### Главное окно

```
┌────────────────────────────────────────────────┐
│ Транслятор Python2 → Python3                    │
├────────────────────────────────────────────────┤
│ [Транслятор] [Анализ]                          │
├────────────────────────────────────────────────┤
│ Python 2 код         │  Python 3 код           │
│                      │                         │
│ print "Hello"        │  print("Hello")        │
│ for i in xrange(10): │  for i in range(10):   │
│                      │                         │
├────────────────────────────────────────────────┤
│ Консоль ошибок                                  │
│ ✓ Анализ завершен успешно (3 преобразования)   │
└────────────────────────────────────────────────┘
```

### Вкладка Анализа

**🌲 Синтаксическое дерево** (графически и текстом)
```
Program
  FunctionDef(name='main')
    If
      Condition
        BinOp(op='>')
          Name('x')
          Literal('5')
      Then
        Print("x > 5")
      Else
        Print("x <= 5")
```

**📊 Таблица токенов** - с цветовой подсветкой
```
Line | Col | Type       | Value
1    | 0   | KEYWORD    | print
1    | 6   | STRING     | "Hello"
```

**📋 Таблица идентификаторов** - с областями видимости
```
Имя    | Область | Вид      | Тип    | Адрес
---------|---------|----------|--------|----------
x        | 0       | variable | int    | (3, 0)
main     | 0       | function | void   | (5, 0)
y        | 1       | variable | str    | (7, 1)
```

## 📦 Установка и Запуск

### Требования

- Python 3.8+
- tkinter (обычно входит в стандартную поставку)

### Запуск

```bash
# Клонирование
git clone https://github.com/Stanlycore/CourseWork.git
cd CourseWork

# Запуск
python main.py
```

## 💡 Примеры

### Пример 1: Базовый print

**Вход:**
```python
print "Hello", "World"
print 42
```

**Выход:**
```python
print("Hello", "World")
print(42)
```

### Пример 2: Вложенные структуры

**Вход:**
```python
class MyClass:
    def method(self):
        for i in xrange(5):
            if i > 2:
                print i
```

**Выход:**
```python
class MyClass:
    def method(self):
        for i in range(5):
            if i > 2:
                print(i)
```

### Пример 3: Исключения

**Вход:**
```python
try:
    risky()
except ValueError, e:
    print "Error:", e
```

**Выход:**
```python
try:
    risky()
except ValueError as e:
    print("Error:", e)
```

## 📚 Документация

**PYTHON2_TO_PYTHON3_GUIDE.md** - подробное описание всех 20+ преобразований с примерами

## 🔍 Обработка Ошибок

Программа находит и подробно описывает:

### Лексические ошибки
- Неизвестные символы
- Некорректные идентификаторы
- Незакрытые строки

### Синтаксические ошибки
- Отсутствие ожидаемых токенов
- Некорректная структура
- Неправильная вложенность

### Семантические ошибки
- Повторное определение переменной
- Использование переменной из другого scope
- Нарушение типов

## 👨‍💻 Автор

Курсовая работа по дисциплине "Теория Языков Программирования"

**Вариант 12** - Трансляция Python 2 → Python 3

## 📄 Лицензия

MIT License

---

**Дополнительная информация:**
- 📖 См. PYTHON2_TO_PYTHON3_GUIDE.md для полного списка преобразований
- 🐛 Об ошибках: создавайте Issue в репозитории
- 🚀 Для развития: создавайте Pull Request'ы
