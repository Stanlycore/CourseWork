# Руководство по использованию

## Быстрый старт

### 1. Запуск приложения

```bash
python main.py
```

### 2. Выбор примера

В верхней части окна находится выпадающий список с 12 примерами:

- **Пример 1-10**: Корректные программы Python 2
- **Пример 11-12**: Программы с ошибками (отмечены ❌)

Выберите пример из списка - код автоматически загрузится в левое поле.

### 3. Анализ и перевод

Нажмите кнопку **"▶ Анализировать и перевести"**

Программа выполнит:
1. Лексический анализ
2. Синтаксический анализ
3. Оптимизацию
4. Генерацию Python 3 кода

### 4. Просмотр результатов

- **Правое верхнее поле**: Переведенный Python 3 код
- **Консоль**: Ход выполнения, ошибки, предупреждения
- **Вкладка "Анализ"**: Подробная информация

## Вкладка "Анализ"

Переключитесь на вкладку "Анализ" для просмотра деталей.

### Синтаксическое дерево

Показывает структуру AST (абстрактного синтаксического дерева):

```
Program
  FunctionDef(name='greet')
    Print
      Literal(value="Hello,")
      Name(id='name')
  For
    Name(id='i')
    Call
      Name(id='range')
      Literal(value=5)
    Call
      Name(id='greet')
      Literal(value="User")
```

### Таблица лексем

Полный список всех токенов с информацией:

| Строка | Столбец | Тип | Значение |
|--------|----------|------|----------|
| 2 | 1 | PRINT | print |
| 2 | 7 | STRING | "Hello, World!" |
| 2 | 23 | NEWLINE | \n |
| 3 | 1 | PRINT | print |

Цветовое кодирование:
- 🟦 **Голубой**: Ключевые слова
- 🟩 **Зеленый**: Идентификаторы
- 🟪 **Оранжевый**: Числа
- 🟪 **Розовый**: Строки
- 🟥 **Красный**: Ошибки

### Таблица идентификаторов

Показывает все идентификаторы с метаданными:

| Имя | Область | Вид | Тип | Значение | Адрес |
|------|----------|------|------|----------|-------|
| greet | 0 | func | auto | - | (45,0) |
| name | 1 | var | auto | - | (78,0) |
| i | 0 | var | auto | - | (15,1) |

**Области видимости:**
- `0` - глобальная область
- `1`, `2`, `3`... - вложенные области
- `1a`, `1b`... - параллельные области на одном уровне

**Адрес** `(bucket, pos)`: положение в хеш-таблице

## Примеры использования

### Пример 1: Успешная трансляция

**Ввод (Python 2):**
```python
def factorial(n):
    if n <= 1:
        return 1
    else:
        return n * factorial(n - 1)

print "5! =", factorial(5)
```

**Консоль:**
```
============================================================
НАЧАЛО АНАЛИЗА
============================================================

[1/5] Лексический анализ...
✔ Найдено 47 токенов

[2/5] Синтаксический анализ...
✔ Синтаксическое дерево построено

[3/5] Оптимизация...
✔ Применено 2 оптимизаций

[4/5] Генерация Python 3 кода...
✔ Код успешно сгенерирован

============================================================
АНАЛИЗ ЗАВЕРШЕН УСПЕШНО!
============================================================
```

**Вывод (Python 3):**
```python
def factorial(n):
    if (n <= 1):
        return 1
    else:
        return (n * factorial((n - 1)))

print("5! =", factorial(5))
```

### Пример 2: Обнаружение ошибок

**Ввод (с ошибкой):**
```python
if x < 10
    print "Less than 10"
```

**Консоль (с ошибками):**
```
============================================================
НАЧАЛО АНАЛИЗА
============================================================

[1/5] Лексический анализ...
✔ Найдено 12 токенов

[2/5] Синтаксический анализ...

✘ Обнаружены синтаксические ошибки:
  • Строка 1:10: Ожидался COLON, но получен NEWLINE
  • Подсказка: Добавьте ':' после условия if
```

## Работа с собственным кодом

1. Очистите левое поле ввода
2. Вставьте свой Python 2 код
3. Нажмите "Анализировать и перевести"
4. Просмотрите результат справа
5. Скопируйте переведенный код

## Советы

### Для лучшего результата

1. **Используйте корректные отступы**: Python требует согласованных отступов (4 пробела или табы)

2. **Закрывайте все скобки и кавычки**: Незакрытые скобки приводят к ошибкам

3. **Не забывайте двоеточия**: После `if`, `else`, `def`, `class`, `for`, `while` нужны `:` 

4. **Используйте примеры**: Изучите 12 встроенных примеров

### Расшифровка сообщений

- ✔ **Зеленые галочки**: Успешное завершение этапа
- ✘ **Красные крестики**: Ошибки, требующие исправления
- ⚠ **Желтые треугольники**: Предупреждения (не критичные)

## Ограничения

Текущая версия не поддерживает:

- `exec` статемент Python 2
- `unicode` строки
- `xrange` (автоматически не преобразуется в `range`)
- Сложные list/dict comprehensions
- Decorators
- Context managers

Эти функции могут быть добавлены в будущих версиях.
